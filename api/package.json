{
  "name": "flx-technical-interview-api",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "server": "json-server --watch db.json --port 3001"
  },
  "browserslist": {
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
